{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
	{{ form_row(form.current_password, {'label': 'Current password: '}) }}
	{{ form_row(form.plainPassword.first, {'label': 'New password: '}) }}
	{{ form_row(form.plainPassword.second, {'label': 'Confirm new password: '}) }}
    <div>
        <input type="submit" value="{{ 'Submit password change'|trans }}" />
    </div>
{{ form_end(form) }}
